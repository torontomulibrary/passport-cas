/**
 * Load dependencies.
 */
let Strategy = require('./strategy');

/**
 * Expose `Strategy` directly from the package.
 */
exports = module.exports = Strategy;

/**
 * Export constructors.
 */
exports.Strategy = Strategy;